import React from 'react';
import PropTypes from 'prop-types';
import { AppWrapper } from '../components/styled/AppWrapper';

const HomePage = () => (
  <>
    <h1>Food for you App</h1>
  </>
);

HomePage.propTypes = {};

export default HomePage;
